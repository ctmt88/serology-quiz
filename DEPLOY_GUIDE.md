# 🚀 血清免疫題庫 - GitHub 部署指南

## 📦 專案檔案

所有檔案已打包在：`serology-quiz-github.tar.gz`

解壓後的結構：
```
github-version/
├── index.html          # 主頁面（30 KB）
├── quiz.html           # 答題頁面（18 KB）
├── data/
│   └── questions.db    # 資料庫（156 KB）
└── README.md           # 說明文件
```

**總大小：約 58 KB（壓縮後）**

---

## 🎯 優勢分析

### ✅ 為什麼使用 SQLite + GitHub？

| 項目 | 單一HTML版本 | SQLite + GitHub版本 |
|------|-------------|-------------------|
| 檔案大小 | 250 KB | 58 KB (壓縮) |
| 可維護性 | ❌ 困難 | ✅ 容易 |
| 自訂功能 | ❌ 需修改HTML | ✅ 只需改資料庫 |
| 新增題目 | ❌ 需重新生成HTML | ✅ SQL INSERT即可 |
| 多人協作 | ❌ 衝突困難 | ✅ Git友善 |
| 版本控制 | ❌ 難追蹤 | ✅ Git完整追蹤 |
| 自訂練習 | ❌ 無法篩選 | ✅ 可選類別/題數 |

---

## 📋 部署到 GitHub Pages

### 步驟1：上傳到 GitHub

```bash
# 解壓檔案
tar -xzf serology-quiz-github.tar.gz
cd github-version

# 初始化 Git
git init
git add .
git commit -m "Initial commit: 血清免疫題庫系統"

# 連接到 GitHub（需先在 GitHub 建立 repository）
git remote add origin https://github.com/您的帳號/serology-quiz.git
git branch -M main
git push -u origin main
```

### 步驟2：啟用 GitHub Pages

1. 到專案的 **Settings** → **Pages**
2. **Source** 選擇 `main` branch
3. **Folder** 選擇 `/ (root)`
4. 點擊 **Save**
5. 等待幾分鐘後即可訪問

**網址格式：** `https://您的帳號.github.io/serology-quiz/`

---

## 🛠️ 本地測試

在上傳前，先在本地測試：

### 使用 Python

```bash
cd github-version
python3 -m http.server 8000
```

### 使用 Node.js

```bash
cd github-version
npx http-server
```

### 使用 VS Code

安裝 **Live Server** 擴充功能，右鍵點擊 `index.html` → **Open with Live Server**

**瀏覽器打開：** `http://localhost:8000`

---

## 💡 功能展示

### 首頁 (index.html)

```
┌─────────────────────────────────────┐
│    🧪 血清免疫題庫                  │
│        自訂練習設定                 │
│                                     │
│  選擇題數：[20] 題                  │
│  [10] [20] [30] [50] [全部]        │
│                                     │
│  選擇類別（全選）                   │
│  ☑ 基礎免疫機制                    │
│  ☑ T細胞免疫                       │
│  ☑ 自體免疫疾病                    │
│  ☑ 實驗技術                        │
│  ...                                │
│                                     │
│     [開始練習 🚀]                   │
│                                     │
│  💡 使用說明                        │
│  📊 題庫總數：160 題               │
└─────────────────────────────────────┘
```

### 答題頁 (quiz.html)

```
┌─────────────────────────────────────┐
│ [1/20]    [🔄 重新設定]  [顯示答案] │
│                                     │
│           第 1 題                   │
│      基礎免疫機制                   │
│                                     │
│  下列何者不屬於抗原呈獻細胞？      │
│                                     │
│  ┌─────────┐  ┌─────────┐         │
│  │(A) ...  │  │(B) ...  │         │
│  └─────────┘  └─────────┘         │
│  ┌─────────┐  ┌─────────┐         │
│  │(C) ...  │  │(D) ...✓ │         │
│  └─────────┘  └─────────┘         │
│                                     │
│        [查看詳解]                   │
│                                     │
│    [上一題]      [下一題]           │
└─────────────────────────────────────┘
```

---

## 🔧 自訂與修改

### 新增題目

#### 方法1：使用 DB Browser for SQLite

1. 下載安裝 [DB Browser for SQLite](https://sqlitebrowser.org/)
2. 開啟 `data/questions.db`
3. 點擊 **Browse Data** → **questions**
4. 點擊 **Insert Record** 新增題目
5. 填寫欄位並儲存
6. 推送到 GitHub

#### 方法2：使用 Python 腳本

```python
import sqlite3

# 連接資料庫
conn = sqlite3.connect('data/questions.db')
cursor = conn.cursor()

# 新增題目
cursor.execute('''
    INSERT INTO questions 
    (question_number, category, question_text, 
     option_a, option_b, option_c, option_d, 
     correct_answer, explanation)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
''', (
    161,                    # 題號
    "新的類別",            # 類別
    "這是新的題目？",      # 題目
    "選項A內容",           # 選項A
    "選項B內容",           # 選項B
    "選項C內容",           # 選項C
    "選項D內容",           # 選項D
    "C",                   # 正確答案
    "這是詳細解析..."     # 解析
))

conn.commit()
conn.close()
print("✅ 題目新增成功！")
```

### 新增圖片題

```python
import base64

# 讀取圖片
with open('圖片路徑.png', 'rb') as f:
    image_data = f.read()
    base64_image = base64.b64encode(image_data).decode('utf-8')

# 插入時加上圖片
cursor.execute('''
    INSERT INTO questions 
    (..., image_base64)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
''', (..., f"data:image/png;base64,{base64_image}"))
```

---

## 📊 資料庫查詢範例

### 查看所有類別

```sql
SELECT DISTINCT category, COUNT(*) as count 
FROM questions 
GROUP BY category 
ORDER BY count DESC;
```

### 查詢特定類別的題目

```sql
SELECT question_number, question_text 
FROM questions 
WHERE category = '基礎免疫機制';
```

### 更新題目

```sql
UPDATE questions 
SET explanation = '新的詳細解析...' 
WHERE id = 1;
```

### 刪除題目

```sql
DELETE FROM questions 
WHERE id = 1;
```

---

## 🎨 自訂樣式

修改 `index.html` 或 `quiz.html` 中的 CSS：

```css
/* 修改主題顏色 */
body {
    background: linear-gradient(135deg, #你的顏色1, #你的顏色2);
}

/* 修改按鈕顏色 */
.btn {
    background: linear-gradient(135deg, #你的顏色1, #你的顏色2);
}
```

---

## 🔍 疑難排解

### 問題1：無法載入資料庫

**症狀：** 頁面顯示「載入失敗」

**解決：**
- 確認 `data/questions.db` 存在
- 使用 HTTP 伺服器運行（不能直接開啟 HTML）
- 檢查瀏覽器 Console 的錯誤訊息

### 問題2：GitHub Pages 404 錯誤

**症狀：** 訪問網址顯示 404

**解決：**
- 確認 GitHub Pages 已啟用
- 確認選擇正確的 branch (main)
- 等待幾分鐘讓 GitHub 建置完成
- 檢查檔案名稱是否正確（區分大小寫）

### 問題3：題目不隨機

**症狀：** 每次開啟順序相同

**解決：**
- 重新整理頁面 (F5)
- 檢查 localStorage 是否有快取
- 清除瀏覽器快取

---

## 📈 效能優化

### 1. 壓縮資料庫

```bash
sqlite3 questions.db "VACUUM;"
```

### 2. 使用 CDN

SQL.js 已使用 CDN，載入速度快

### 3. 啟用 GZIP

GitHub Pages 自動啟用 GZIP 壓縮

---

## 🔐 安全性

- ✅ 純前端應用，無後端
- ✅ 資料存在 SQLite，無需 API
- ✅ LocalStorage 僅存設定
- ✅ 無個人資料收集
- ✅ 可完全離線使用

---

## 📱 跨平台支援

- 🖥️ **桌面瀏覽器：** Chrome, Firefox, Edge, Safari
- 📱 **手機瀏覽器：** iOS Safari, Android Chrome
- 💻 **平板：** 完整支援

---

## 🎓 學習資源

### SQL.js 文件
https://sql.js.org/

### SQLite 語法
https://www.sqlite.org/lang.html

### GitHub Pages 文件
https://docs.github.com/en/pages

---

## 🤝 維護建議

### 定期備份

```bash
# 備份資料庫
cp data/questions.db data/questions_backup_$(date +%Y%m%d).db

# 推送到 GitHub
git add data/questions_backup_*.db
git commit -m "Backup database"
git push
```

### 版本標籤

```bash
# 建立版本標籤
git tag -a v1.0.0 -m "Version 1.0.0"
git push origin v1.0.0
```

---

## 📞 技術支援

遇到問題？

1. 查看 README.md
2. 查看 GitHub Issues
3. 開啟新的 Issue
4. 聯絡維護者

---

## 🎉 總結

使用 SQLite + GitHub 的優勢：

✅ **易於維護** - 資料與程式分離
✅ **功能強大** - SQL 查詢靈活
✅ **版本控制** - Git 完整追蹤
✅ **自訂練習** - 可選類別和題數
✅ **免費託管** - GitHub Pages 永久免費
✅ **快速部署** - 幾分鐘即可上線

**立即開始使用，享受自訂練習的便利！** 🚀
